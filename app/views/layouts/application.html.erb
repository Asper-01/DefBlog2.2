<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8">
    <meta name="turbo-cache-control" content="no-cache">
    <meta name="turbo-visit-control" content="reload">
    <title><%= content_for?(:title) ? yield(:title) : "DefBlog défense" %></title>
    <title>DefBlog blog défense </title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Lien vers les styles -->
    <%= stylesheet_link_tag "application", media: 'all', "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Ajout de Font Awesome via CDN -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">

    <!-- Importation de l'importmap et des scripts -->

    <!-- Script pour la gestion des cookies -->
    <%= javascript_include_tag 'cookies_eu', 'data-turbo-track': 'reload' %>

    <!-- Lien vers le fichier JS de Bootstrap -->
    <script type="module" src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script type="module" src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.min.js"></script>

    <link rel="icon" href="<%= asset_path 'logo.png' %>" type="image/x-icon">
    <%= yield :page_header %>

    <!-- Google Tag Manager -->
    <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
    new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
    j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
    'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
    })(window,document,'script','dataLayer','GTM-MZZ8NL73');</script>
    <!-- End Google Tag Manager -->
  </head>

  <body>
    <!-- Google Tag Manager (noscript) -->
    <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-MZZ8NL73"
    height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
    <!-- End Google Tag Manager (noscript) -->

    <!-- Navigation -->
    <%= render "shared/navbar" %>

    <!-- Gestion des cookies -->
    <%= render 'cookies_eu/consent_banner' %>

    <!-- Flash messages (si existants) -->
    <%= render "shared/flashes" %>

    <!-- Contenu principal de la page -->
    <%= yield %>

    <!-- Contenu du footer -->
    <%= render 'shared/footer' %>
  </body>
</html>
