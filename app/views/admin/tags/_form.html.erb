<%= form_with(model: tag, local: false, data: { controller: "toggle" }) do |form| %>
  <div class="form-group">
    <%= form.label :name, "Nom du tag", class: "form-label" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>
  <div class="d-flex justify-content-between mt-2">
    <%= form.submit "Mettre à jour le tag", class: "btn btn-primary" %>
    <%= link_to 'Annuler', "#", class: 'btn btn-secondary', data: { action: "toggle#hide" } %>
  </div>
<% end %>
